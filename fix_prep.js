const fs = require('fs');

const path = 'src/presentation/pages/PreparationPage.tsx';
let code = fs.readFileSync(path, 'utf8');

// We need to implement a continuous list approach for PreparationPage.
// ... 
